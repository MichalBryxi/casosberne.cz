{{#if isFormDisplayed}}
  <h4>
    {{t 'project.newPost.heading' hashtag=project.hashtag}}
  </h4>

  {{#md-modal close='toggleForm' class="modal-fixed-footer"}}
    <div class="modal-content">
      <div class="input-field">
        <input id="new-post-message" type="text" maxlength={{maxLength}} length={{maxLength}} />
        <label for="new-post-message">{{t 'project.newPost.message'}}</label>
      </div>

      {{#x-droplet}}{{/x-droplet}}
    </div>

    <div class="modal-footer">
      <a class="new-post-send" {{action sendForm}}>
        {{t 'project.newPost.send'}}
      </a>
      <a class="new-post-cancel" {{action toggleForm}}>
        {{t 'project.newPost.cancel'}}
      </a>
    </div>
  {{/md-modal}}
{{/if}}
